{% extends 'base.html.twig' %}

{% block title %}Create a new Record |
	{{ parent() }}
{% endblock %}

{% block body %}

	<h1>{{ title }}</h1>

	{# TODO: say somethink an comment #}
	<div class="d-flex w-100">
		<div class="w-50">
			<img src="{{asset('img/vinylbg.webp')}}" alt="some vinyl" class="w-100">
		</div>
		<div>
			Our schweet track list TODO

			<ul>
				{% for track in tracks %}
					<li>
						{{track.artist}}
						-
						{{track.song|upper}}
					</li>
				{% endfor %}
			</ul>


		</div>
	</div>


{% endblock %}
